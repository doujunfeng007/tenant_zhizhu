<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.minigod.zero.manage.mapper.SnCouponManageMatchMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.minigod.zero.manage.entity.SnCouponManageMatchEntity">
        <id column="id" property="id"/>
        <result column="serial_num" property="serialNum"/>
        <result column="code" property="code"/>
        <result column="user_id" property="userId"/>
        <result column="channel_id" property="channelId"/>
        <result column="record_date" property="recordDate"/>
        <result column="use_date" property="useDate"/>
        <result column="open_date" property="openDate"/>
        <result column="deposit_date" property="depositDate"/>
        <result column="deposit_money" property="depositMoney"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="create_dept" property="createDept"/>
        <result column="status" property="status"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, serial_num, code, user_id, channel_id, record_date, use_date,
        open_date, deposit_date, deposit_money, create_time, update_time,
        create_user, update_user, is_deleted, tenant_id, create_dept, status
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sn_coupon_manage_match
        WHERE is_deleted = 0
        <if test="code != null and code != ''">
            AND code = #{code}
        </if>
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="channelId != null and channelId != ''">
            AND channel_id = #{channelId}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 根据序列号查询匹配表数据 -->
    <select id="findSnCouponManageMatchSerialNumData" resultType="com.minigod.zero.manage.vo.SnCouponManageMatchVO">
        SELECT e.`serial_num`,
               e.use_uid AS userId,
               u.user_source_channel_id AS channelId,
               e.record_date,
               e.use_date,
               o.open_date,
               d.created_time AS depositDate,
               d.deposit_money
        FROM sn_coupon_exchange_code e
        LEFT JOIN open_user_info o ON o.user_id = e.use_uid
        LEFT JOIN user_info u ON u.user_id = e.use_uid
        LEFT JOIN sec_first_deposit_record d ON d.user_id = e.use_uid
        WHERE e.serial_num in
        <foreach collection="serialNumList" item="serialNum" open="(" separator="," close=")">
            #{serialNum}
        </foreach>
    </select>

    <!-- 根据兑换码查询匹配表数据 -->
    <select id="findSnCouponManageMatchCodeData" resultType="com.minigod.zero.manage.vo.SnCouponManageMatchVO">
        SELECT e.`serial_num`,
        e.use_uid AS userId,
        u.user_source_channel_id AS channelId,
        e.record_date,
        e.use_date,
        o.open_date,
        d.created_time AS depositDate,
        d.deposit_money
        FROM sn_coupon_exchange_code e
        LEFT JOIN open_user_info o ON o.user_id = e.use_uid
        LEFT JOIN user_info u ON u.user_id = e.use_uid
        LEFT JOIN sec_first_deposit_record d ON d.user_id = e.use_uid
        WHERE e.code in
        <foreach collection="codeList" item="code" open="(" separator="," close=")">
            #{code}
        </foreach>
    </select>

    <!-- 查询用户的匹配记录 -->
    <select id="queryByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sn_coupon_manage_match
        WHERE is_deleted = 0
        AND user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 根据激活码查询匹配记录 -->
    <select id="getByCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sn_coupon_manage_match
        WHERE is_deleted = 0
        AND code = #{code}
        LIMIT 1
    </select>

    <!-- 更新用户开户入金信息 -->
    <update id="updateAccountInfo" parameterType="com.minigod.zero.manage.entity.SnCouponManageMatchEntity">
        UPDATE sn_coupon_manage_match
        <set>
            <if test="openDate != null">
                open_date = #{openDate},
            </if>
            <if test="depositDate != null">
                deposit_date = #{depositDate},
            </if>
            <if test="depositMoney != null">
                deposit_money = #{depositMoney},
            </if>
            <if test="useDate != null">
                use_date = #{useDate},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
        AND is_deleted = 0
    </update>

</mapper>
