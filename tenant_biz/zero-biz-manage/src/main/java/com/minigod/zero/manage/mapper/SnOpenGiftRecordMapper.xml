<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.minigod.zero.manage.mapper.SnOpenGiftRecordMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.minigod.zero.manage.entity.SnOpenGiftRecordEntity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="opr_id" property="oprId"/>
        <result column="opr_name" property="oprName"/>
        <result column="opr_reason" property="oprReason"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_user" property="createUser"/>
        <result column="update_user" property="updateUser"/>
        <result column="is_deleted" property="isDeleted"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="create_dept" property="createDept"/>
        <result column="status" property="status"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, opr_id, opr_name, opr_reason, create_time, update_time,
        create_user, update_user, is_deleted, tenant_id, create_dept, status
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sn_open_gift_record
        WHERE is_deleted = 0
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="oprId != null">
            AND opr_id = #{oprId}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 查询礼包列表 -->
    <select id="queryOpenChannelGiftList" resultMap="BaseResultMap">

    </select>

    <!-- 查询用户的礼包记录 -->
    <select id="queryByUserId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sn_open_gift_record
        WHERE is_deleted = 0
        AND user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 查询操作人员的发放记录 -->
    <select id="queryByOprId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sn_open_gift_record
        WHERE is_deleted = 0
        AND opr_id = #{oprId}
        ORDER BY create_time DESC
    </select>

</mapper>
