#服务器端口
server:
  port: 8000

#数据源配置
spring:
  datasource:
    url: ${zero.datasource.bpmn.prd.url}
    username: ${zero.datasource.bpmn.prd.username}
    password: ${zero.datasource.bpmn.prd.password}
