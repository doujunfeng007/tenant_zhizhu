#数据源配置
spring:
  datasource:
    url: ${zero.datasource.uat.url}
    username: ${zero.datasource.uat.username}
    password: ${zero.datasource.uat.password}

#第三方登陆
social:
  enabled: true
  domain: http://127.0.0.1:1888
